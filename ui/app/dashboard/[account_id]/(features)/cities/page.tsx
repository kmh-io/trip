import { Button } from "@/components/ui/button";

export default function CityPage() {
  return (
    <main className="h-full flex justify-center items-center">
      <Button variant={"outline"}>Cities Management Page</Button>
    </main>
  );
}
