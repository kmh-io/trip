import { Button } from "@/components/ui/button";

export default function Setting() {
  return (
    <main className="h-full flex justify-center items-center">
      <Button variant={"outline"}>Setting Page</Button>
    </main>
  );
}
