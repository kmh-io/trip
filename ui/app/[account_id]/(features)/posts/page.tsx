import { Button } from "@/components/ui/button";

export default function Post() {
  return (
    <main className="h-full flex justify-center items-center">
      <Button variant={"outline"}>Post Management Page</Button>
    </main>
  );
}
